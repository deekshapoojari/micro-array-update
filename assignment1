#include <stdio.h>

int main()
{
int T;
int K,N;
int i;
for(j=0;j<N;j++)
{
    scanf("%d%d",&K,&N);
    for(j=0;j<N;j++)
    {
        scanf("%d",&a[j]);
    }
    for(i=0;i<N;i++)
    {
        res=K-a[i];
    }
    printf(res);

    return 0;
}
